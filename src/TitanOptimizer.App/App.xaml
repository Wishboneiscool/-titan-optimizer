<Application x:Class="TitanOptimizer.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <SolidColorBrush x:Key="PageBackground" Color="#0B1020" />
        <SolidColorBrush x:Key="PanelBackground" Color="#121A2E" />
        <SolidColorBrush x:Key="PanelBorder" Color="#263453" />
        <SolidColorBrush x:Key="PrimaryText" Color="#F4F7FF" />
        <SolidColorBrush x:Key="SecondaryText" Color="#9BA8C7" />
        <SolidColorBrush x:Key="Accent" Color="#66D9EF" />
        <SolidColorBrush x:Key="Success" Color="#7EE787" />
        <SolidColorBrush x:Key="Warning" Color="#F2CC60" />
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource PrimaryText}" />
            <Setter Property="FontFamily" Value="Segoe UI" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource PrimaryText}" />
            <Setter Property="Background" Value="#1D2A48" />
            <Setter Property="BorderBrush" Value="{StaticResource PanelBorder}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="14,8" />
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="FontSize" Value="13" />
        </Style>
    </Application.Resources>
</Application>
